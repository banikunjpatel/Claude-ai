/** Re-export useAuth from AuthProvider for convenience.
 *
 * Allows imports from either:
 *   import { useAuth } from '@/auth/AuthProvider'
 *   import { useAuth } from '@/auth/useAuth'
 */
export { useAuth } from './AuthProvider'
